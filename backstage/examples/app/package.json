{
  "name": "rampart-example-app",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal production Backstage app wiring the rampart frontend + backend plugins.",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspace app build && yarn workspace backend build",
    "start-frontend": "yarn workspace app start",
    "start-backend": "yarn workspace backend start",
    "tsc": "tsc",
    "clean": "yarn workspace app clean && yarn workspace backend clean"
  },
  "devDependencies": {
    "@backstage/cli": "^0.34.6",
    "typescript": "5.6.3"
  }
}
