{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "backstage": {
    "role": "backend"
  },
  "main": "dist/index.cjs.js",
  "scripts": {
    "build": "backstage-cli package build",
    "build-image": "backstage-cli package build-image",
    "start": "backstage-cli package start",
    "clean": "backstage-cli package clean",
    "test": "backstage-cli package test",
    "lint": "backstage-cli package lint"
  },
  "dependencies": {
    "@backstage/backend-defaults": "^0.14.2",
    "@backstage/config": "^1.2.0",
    "@backstage/plugin-app-backend": "^0.5.13",
    "@ugurcan-aytar/backstage-plugin-rampart-backend": "workspace:^",
    "better-sqlite3": "^12.9.0"
  },
  "devDependencies": {
    "@backstage/cli": "^0.34.6",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "files": [
    "dist"
  ]
}
